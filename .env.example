# vark configuration. Copy to .env and fill in the values.
# Every VARK_* variable the backend reads (see backend/internal/config/config.go)
# is listed below with its real default in a comment.

# --- Core -------------------------------------------------------------------

# Externally reachable base URL. Required in production (OIDC redirect/logout
# URLs are built relative to it); leave empty for dev mode (default "").
VARK_PUBLIC_URL=https://vark.example.com

# HTTP listen address (default ":8080"). Dev auth REQUIRES this to be a
# loopback address (e.g. "127.0.0.1:8080") — the boot check refuses to start
# otherwise.
# VARK_ADDR=:8080

# Long random string used to sign session cookies. Required — the process
# refuses to start without it. Generate one with e.g. `openssl rand -hex 32`.
VARK_SESSION_SECRET=change-me-to-a-long-random-string

# --- Authentication -----------------------------------------------------
# One of: oidc | dev. No default — the process refuses to start if unset.
VARK_AUTH_MODE=oidc

# OIDC block, required when VARK_AUTH_MODE=oidc.
VARK_OIDC_ISSUER=https://auth.example.com/application/o/vark/
VARK_OIDC_CLIENT_ID=
VARK_OIDC_CLIENT_SECRET=
VARK_OIDC_REDIRECT_URL=https://vark.example.com/api/auth/callback
# Optional: where the IdP sends the browser back to after logout (default "").
VARK_OIDC_POST_LOGOUT_REDIRECT_URL=https://vark.example.com/
# Optional: OIDC group name that grants admin (default "", meaning no group
# check — every authenticated user is admin; vark is single-user by design).
VARK_OIDC_ADMIN_GROUP=vark-admins

# --- Storage ------------------------------------------------------------

# Path to the SQLite database file (default "/data/vark.db"). Holds settings,
# the job queue, and every video's watch/favorite/tombstone state — back this
# file up, see README.
VARK_DB_PATH=/data/vark.db

# Root directory downloaded media is written to, as <dir>/<channel>/<video>/
# (default "/data/media").
VARK_MEDIA_DIR=/data/media

# Writable directory the yt-dlp binary is fetched/self-updated into at
# runtime (default "/data/bin").
VARK_YTDLP_DIR=/data/bin

# --- Logging --------------------------------------------------------------

# Log verbosity: debug | info | warn | error (default "info").
# VARK_LOG_LEVEL=info

# --- Local development without OIDC ----------------------------------------
# Enables an unauthenticated auto-login as a fixed local admin user. The
# process refuses to start in this mode unless VARK_ADDR/VARK_PUBLIC_URL are
# loopback-only, so this can never accidentally become reachable over the
# network.
# VARK_AUTH_MODE=dev
# VARK_ADDR=127.0.0.1:8080
# VARK_PUBLIC_URL=http://localhost:8080

# Dev auto-login identity overrides (defaults shown; only read in dev mode).
# VARK_DEV_USER_SUBJECT=dev-admin
# VARK_DEV_USER_USERNAME=dev
# VARK_DEV_USER_EMAIL=dev@example.local
# VARK_DEV_USER_NAME=Dev Admin
