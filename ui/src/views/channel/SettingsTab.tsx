import type { ChannelDetail } from "../../api/types";

// SettingsTab — stub (Task 14 fills it in).
export function SettingsTab(_: { detail: ChannelDetail; onChanged: () => void; onDeleted: () => void }) {
  return null;
}
